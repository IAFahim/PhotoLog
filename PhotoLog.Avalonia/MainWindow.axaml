<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhotoLog.Avalonia"
        mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
        x:Class="PhotoLog.Avalonia.MainWindow"
        Width="1200" Height="840"
        Background="Transparent"
        TransparencyLevelHint="Mica, AcrylicBlur, None"
        Title="PhotoLog">

  <DockPanel Margin="16">

    <!-- header -->
    <StackPanel DockPanel.Dock="Top" Spacing="8">
      <TextBlock Text="PhotoLog" FontSize="26" FontWeight="SemiBold"/>
      <TextBlock Classes="hint" FontSize="13"
                 Text="Stamp the date (and your address text, if given) on each photo's top-right corner."/>
      <Grid ColumnDefinitions="*,Auto,Auto" Margin="0,4,0,0">
        <TextBox x:Name="FolderBox" PlaceholderText="/path/to/photos — or click Browse" KeyDown="Folder_KeyDown"/>
        <Button Grid.Column="1" Content="Browse…" Margin="6,0,0,0" Click="Browse_Click"/>
        <Button Grid.Column="2" Content="Load" Margin="6,0,0,0" Classes="accent" Click="Load_Click"/>
      </Grid>
      <TextBlock x:Name="Status" Classes="hint"
                 Text="Pick a folder with Browse, or type its path and press Enter."/>
      <Border Classes="card">
      <Grid ColumnDefinitions="Auto,Auto,Auto,*,Auto,Auto">
        <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
          <TextBlock Classes="label" Text="Override date:"/>
          <CalendarDatePicker x:Name="DatePart" Width="150"
                              ToolTip.Tip="Leave empty to stamp each photo's own date. Picking a date replaces only the day — the time stays each photo's own."
                              SelectedDateChanged="DateChanged"/>
        </StackPanel>
        <Button Grid.Column="1" Content="Clear" Margin="6,0,0,0" VerticalAlignment="Center"
                ToolTip.Tip="Back to each photo's own date"
                Click="ClearDate_Click"/>
        <TextBlock Grid.Column="2" Classes="label" Text="Address (optional):" Margin="18,0,8,0"/>
        <TextBox Grid.Column="3" x:Name="AddrBox" AcceptsReturn="True" Height="72"
                 LostFocus="Addr_LostFocus"
                 PlaceholderText="1521 Meander Rd&#10;Timmonsville SC 29161&#10;United States"/>
        <ComboBox Grid.Column="4" x:Name="StyleCombo" Width="240" Margin="18,0,0,0" VerticalAlignment="Center"
                  ToolTip.Tip="Stamp style — applies to every image, selected or not"
                  SelectionChanged="StyleChanged"/>
        <Button Grid.Column="5" Content="Refresh previews" Margin="8,0,0,0" VerticalAlignment="Center"
                Click="Refresh_Click"/>
      </Grid>
      </Border>
    </StackPanel>

    <!-- footer -->
    <StackPanel DockPanel.Dock="Bottom" Spacing="6" Margin="0,14,0,0">
      <Grid ColumnDefinitions="Auto,*,Auto,Auto">
        <TextBlock Classes="label" Text="Output folder:" Margin="0,0,8,0"/>
        <TextBox Grid.Column="1" x:Name="OutBox"/>
        <Button Grid.Column="2" Content="Browse…" Margin="8,0,0,0" Click="OutBrowse_Click"/>
        <Button Grid.Column="3" x:Name="ApplyBtn" Content="Apply" Margin="8,0,0,0" Classes="accent"
                Click="Apply_Click"/>
      </Grid>
      <TextBlock x:Name="Result" Classes="hint" TextWrapping="Wrap"/>
    </StackPanel>

    <!-- grid + preview -->
    <Grid ColumnDefinitions="3*,2*" Margin="0,14,0,0">
      <Border Classes="card">
      <DockPanel>
        <Grid DockPanel.Dock="Top" ColumnDefinitions="*,Auto" Margin="0,0,0,6">
          <TextBlock x:Name="CountText" Text="0/0 selected" VerticalAlignment="Center"/>
          <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
            <Button Content="Select all" Click="SelectAll_Click"/>
            <Button Content="Select none" Click="SelectNone_Click"/>
          </StackPanel>
        </Grid>
        <ScrollViewer>
          <ItemsControl x:Name="PhotoGrid">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="local:PhotoItem">
                <Border Width="150" Height="140" Margin="5" CornerRadius="8" ClipToBounds="True"
                        Background="#20808080" Cursor="Hand" ToolTip.Tip="{Binding Tip}"
                        PointerPressed="Cell_Click">
                  <Panel>
                    <Image Source="{Binding Thumb}" Stretch="UniformToFill"/>
                    <!-- checkbox indicator: empty box / green check -->
                    <Border Width="22" Height="22" Margin="6" HorizontalAlignment="Left" VerticalAlignment="Top"
                            CornerRadius="5" BorderThickness="2" BorderBrush="#333333" Background="White"
                            IsVisible="{Binding !Selected}"/>
                    <Border Width="22" Height="22" Margin="6" HorizontalAlignment="Left" VerticalAlignment="Top"
                            CornerRadius="5" BorderThickness="2" BorderBrush="#166534" Background="#22C55E"
                            IsVisible="{Binding Selected}">
                      <TextBlock Text="✓" Foreground="White" FontWeight="Bold" FontSize="14"
                                 HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <Border VerticalAlignment="Bottom" Background="#8C000000" Padding="4,2">
                      <TextBlock Text="{Binding Name}" Foreground="White" FontSize="11"
                                 TextTrimming="CharacterEllipsis"/>
                    </Border>
                  </Panel>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </DockPanel>
      </Border>

      <!-- star row so a tall portrait preview shrinks to fit instead of overflowing -->
      <Border Grid.Column="1" Classes="card" Margin="14,0,0,0">
      <Grid RowDefinitions="*,Auto,Auto,Auto,Auto">
        <Image x:Name="PreviewImage" Stretch="Uniform" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" x:Name="PreviewName" TextAlignment="Center" TextWrapping="Wrap" Opacity="0.85"
                   Text="Click an image to preview it and toggle selection"/>
        <Grid Grid.Row="2" ColumnDefinitions="*,Auto" Margin="0,8,0,0">
          <TextBox x:Name="CaptionBox" PlaceholderText="Caption (stamped below the address; edit or clear freely)"
                   LostFocus="Caption_LostFocus" IsEnabled="False"/>
          <Button Grid.Column="1" x:Name="CaptionBtn" Content="Caption" Margin="6,0,0,0"
                  IsEnabled="False" Click="Caption_Click"/>
        </Grid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8" Margin="0,8,0,0">
          <Button x:Name="SaveOneBtn" Content="Save this image" IsEnabled="False" Click="SaveOne_Click"/>
          <Button x:Name="CaptionAllBtn" Content="Caption selected" IsEnabled="False" Click="CaptionAll_Click"/>
        </StackPanel>
        <TextBlock Grid.Row="4" x:Name="AiStatus" Classes="hint" Margin="0,8,0,0"
                   TextAlignment="Center" TextWrapping="Wrap"/>
      </Grid>
      </Border>
    </Grid>

  </DockPanel>
</Window>
